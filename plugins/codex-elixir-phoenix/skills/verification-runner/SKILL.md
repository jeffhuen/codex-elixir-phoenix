---
name: verification-runner
description: "Run project-aware verification loop. Reads mix.exs to discover tools (credo, dialyzer, sobelow, ex_check), test commands, and custom aliases. Use proactively after code changes."
metadata:
  short-description: "Run project-aware verification loop. Reads mix.exs to discover"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/verification-runner.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Verification Runner

This skill is the Codex plugin entry point for the upstream Claude agent
`verification-runner`.

## Workflow

1. Read `../../agent-sources/verification-runner.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/verification-runner.md`, translated for Codex
tool names and project conventions.
