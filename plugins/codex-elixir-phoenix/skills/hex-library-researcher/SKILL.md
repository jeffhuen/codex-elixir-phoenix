---
name: hex-library-researcher
description: "Researches Elixir libraries on hex.pm. Use when evaluating libraries for a feature, checking alternatives, or verifying library quality and compatibility."
metadata:
  short-description: "Researches Elixir libraries on hex.pm"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/hex-library-researcher.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Hex Library Researcher

This skill is the Codex plugin entry point for the upstream Claude agent
`hex-library-researcher`.

## Workflow

1. Read `../../agent-sources/hex-library-researcher.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/hex-library-researcher.md`, translated for Codex
tool names and project conventions.
