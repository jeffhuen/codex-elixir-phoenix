---
name: hex-deps-triager
description: "Triage Hex supply-chain audit findings in Elixir/Phoenix — review diff windows and metadata to produce structured security verdicts. Use after /phx:deps-audit."
metadata:
  short-description: "Triage Hex supply-chain audit findings in Elixir/Phoenix"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/hex-deps-triager.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Hex Deps Triager

This skill is the Codex plugin entry point for the upstream Claude agent
`hex-deps-triager`.

## Workflow

1. Read `../../agent-sources/hex-deps-triager.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/hex-deps-triager.md`, translated for Codex
tool names and project conventions.
