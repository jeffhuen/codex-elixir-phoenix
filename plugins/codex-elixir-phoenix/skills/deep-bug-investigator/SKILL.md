---
name: deep-bug-investigator
description: "Deep bug investigation using 4 parallel subagents (reproduction, root cause, impact, fix strategy). Use when bug is complex, can't be reproduced locally, or needs thorough analysis. Spawns fresh-context subagents for each investigation track."
metadata:
  short-description: "Deep bug investigation using 4 parallel subagents"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/deep-bug-investigator.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Deep Bug Investigator

This skill is the Codex plugin entry point for the upstream Claude agent
`deep-bug-investigator`.

## Workflow

1. Read `../../agent-sources/deep-bug-investigator.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/deep-bug-investigator.md`, translated for Codex
tool names and project conventions.
