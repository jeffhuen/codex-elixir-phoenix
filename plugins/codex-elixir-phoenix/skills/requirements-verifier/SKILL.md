---
name: requirements-verifier
description: "Cross-check implementation against task requirements (Linear issue, GitHub issue, plan, spec). Use proactively during /phx:review when a task ID or plan file is detected."
metadata:
  short-description: "Cross-check implementation against task requirements (Linear"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/requirements-verifier.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Requirements Verifier

This skill is the Codex plugin entry point for the upstream Claude agent
`requirements-verifier`.

## Workflow

1. Read `../../agent-sources/requirements-verifier.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/requirements-verifier.md`, translated for Codex
tool names and project conventions.
