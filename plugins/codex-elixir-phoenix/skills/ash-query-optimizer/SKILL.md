---
name: ash-query-optimizer
description: "Ash query optimizer — detects N+1 loads, suggests aggregates over load+Enum, identifies calculation vs load tradeoffs. Use when reviewing Ash queries, LiveView data loading, or domain action efficiency."
metadata:
  short-description: "Ash query optimizer specialist workflow"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/ash-query-optimizer.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Ash Query Optimizer

This skill is the Codex plugin entry point for the upstream Claude agent
`ash-query-optimizer`.

## Workflow

1. Read `../../agent-sources/ash-query-optimizer.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/ash-query-optimizer.md`, translated for Codex
tool names and project conventions.
