---
name: parallel-reviewer
description: "Parallel code review using 4 specialist agents (elixir-reviewer, security-analyzer, testing-reviewer, verification-runner). Use for thorough review of significant changes."
metadata:
  short-description: "Parallel code review using 4 specialist agents"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/parallel-reviewer.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Parallel Reviewer

This skill is the Codex plugin entry point for the upstream Claude agent
`parallel-reviewer`.

## Workflow

1. Read `../../agent-sources/parallel-reviewer.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/parallel-reviewer.md`, translated for Codex
tool names and project conventions.
