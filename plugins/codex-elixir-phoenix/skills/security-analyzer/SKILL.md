---
name: security-analyzer
description: "Security audit specialist for Elixir/Phoenix - authentication, authorization, input validation, OWASP vulnerabilities. Use proactively when implementing auth or handling user input."
metadata:
  short-description: "Security audit specialist for Elixir/Phoenix"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/security-analyzer.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Security Analyzer

This skill is the Codex plugin entry point for the upstream Claude agent
`security-analyzer`.

## Workflow

1. Read `../../agent-sources/security-analyzer.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/security-analyzer.md`, translated for Codex
tool names and project conventions.
