---
name: web-researcher
description: "Fetches and extracts information from web sources efficiently. Optimized for ElixirForum, HexDocs, and GitHub. Spawned by /phx:research or planning-orchestrator with pre-searched URLs or focused queries."
metadata:
  short-description: "Fetches and extracts information from web sources efficiently"
---

<!-- Generated by tools/generate-agent-skills.mjs from agent-sources/web-researcher.md. -->
<!-- Do not edit by hand; edit the source agent Markdown and regenerate. -->

# Web Researcher

This skill is the Codex plugin entry point for the upstream Claude agent
`web-researcher`.

## Workflow

1. Read `../../agent-sources/web-researcher.md`.
2. Apply the Codex compatibility mappings from `../codex-compat/SKILL.md`.
3. Run the specialist workflow inline by default.
4. Use Codex subagents only when the user explicitly asks for subagents,
   delegation, or parallel agent work. If named custom agents are unavailable,
   use a focused built-in `worker` or `explorer` subagent, or run the same
   checklist inline.

## Output

Follow the output contract in `../../agent-sources/web-researcher.md`, translated for Codex
tool names and project conventions.
